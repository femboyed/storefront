import {NextPage} from "next";
import DefaultLayout from "@/layouts/default";

const Home: NextPage = () => {
    return (
        <DefaultLayout>
            Main page (construction)
        </DefaultLayout>
    )
}

export default Home;
